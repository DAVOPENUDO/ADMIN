import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scanroster.applista',
  appName: 'AppLista',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
